# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'AdapterDigital' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AdapterDigital
	pod 'FSCalendar'
	pod 'SwiftDate'
	pod 'DropDown'
	pod 'SwiftyJSON'
	pod 'Presentr'

  target 'AdapterDigitalTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AdapterDigitalUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
